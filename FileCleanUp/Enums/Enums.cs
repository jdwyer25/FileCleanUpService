namespace FileCleanUp.Enums
{
    public class Enums
    {
        public enum LogLevel
        {
            Off,
            Error = 1,
            Trace = 2
        }

        public enum Mode
        {
            Dev,
            Prod = 1
        }
    }
}
